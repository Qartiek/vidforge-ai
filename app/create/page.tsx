"use client";
import {useState} from "react";
export default function Create(){const [company,setCompany]=useState("");const [angle,setAngle]=useState("");const [result,setResult]=useState<any>(null);async function start(){const r=await fetch("/api/generate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({company,angle,durationMinutes:8,language:"English"})});setResult(await r.json())}return <main className="container"><nav className="nav"><div className="logo">VidForge AI</div><span className="muted">Finance Studio</span></nav><section style={{padding:"45px 0"}}><span className="badge">COMPANY FINANCE</span><h1 style={{fontSize:42}}>Build a finance video</h1><p className="muted">Start with a company and angle. VidForge will turn the brief into a structured production job.</p><div className="card" style={{maxWidth:760,marginTop:28}}><label>Company</label><input value={company} onChange={e=>setCompany(e.target.value)} placeholder="e.g. NVIDIA" style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><label style={{display:"block",marginTop:18}}>Video angle</label><input value={angle} onChange={e=>setAngle(e.target.value)} placeholder="How NVIDIA makes money and why it dominates AI chips" style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><button className="cta" style={{border:0,marginTop:18,cursor:"pointer"}} onClick={start} disabled={!company.trim()}>Start Research Job</button></div>{result&&<div className="card" style={{maxWidth:760,marginTop:24}}><h3>Job queued</h3><p className="muted">Job ID: {result.jobId}</p><p className="muted">Next: {result.next?.join(" → ")}</p></div>}</section></main>}