"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function Signup(){const router=useRouter();const [name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/auth/signup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:name.trim(),email:email.trim().toLowerCase(),password})});const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.error||"Signup failed");return;}router.push("/dashboard");router.refresh();}catch{setError("Network error. Please try again.");}finally{setBusy(false);}}
return <main className="container"><section className="hero"><span className="badge">START CREATING</span><h1>Create your account.</h1><p>Build and automate your YouTube content from one workspace.</p><form className="card" style={{maxWidth:460,margin:"30px auto",textAlign:"left"}} onSubmit={submit}><label>Name</label><input value={name} onChange={e=>setName(e.target.value)} style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><label style={{display:"block",marginTop:16}}>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><label style={{display:"block",marginTop:16}}>Password</label><input required minLength={12} type="password" value={password} onChange={e=>setPassword(e.target.value)} style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><small style={{display:"block",marginTop:8,opacity:.7}}>Password must be at least 12 characters.</small>{error&&<p style={{color:"#f87171"}}>{error}</p>}<button className="cta" disabled={busy} style={{border:0,marginTop:16,cursor:"pointer"}}>{busy?"Creating…":"Create account"}</button></form></section></main>}