"use client";
import {useState} from "react";
export default function Content(){const [company,setCompany]=useState("");const [angle,setAngle]=useState("");const [result,setResult]=useState<any>(null);async function run(){const r=await fetch("/api/content",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({company,angle})});setResult(await r.json())}return <main className="container"><nav className="nav"><div className="logo">NOVYN</div><span className="muted">Content Engine</span></nav><section style={{padding:"45px 0"}}><span className="badge">HOOK + SCRIPT ENGINE</span><h1 style={{fontSize:42}}>Turn research into a story.</h1><p className="muted">Generate multiple hooks and a production-ready script blueprint before connecting the model provider.</p><div className="card" style={{maxWidth:760,marginTop:28}}><input value={company} onChange={e=>setCompany(e.target.value)} placeholder="Company name" style={{width:"100%",padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><input value={angle} onChange={e=>setAngle(e.target.value)} placeholder="Video angle" style={{width:"100%",marginTop:12,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><button className="cta" style={{border:0,marginTop:16,cursor:"pointer"}} onClick={run} disabled={!company.trim()}>Generate Blueprint</button></div>{result?.hooks&&<div className="card" style={{maxWidth:760,marginTop:24}}><h3>Hook options</h3>{result.hooks.map((x:string,i:number)=><p key={x}><b>{i+1}.</b> {x}</p>)}<h3>Script structure</h3>{result.scriptBlueprint.structure.map((x:string,i:number)=><p className="muted" key={x}>{i+1}. {x}</p>)}</div>}</section></main>}
