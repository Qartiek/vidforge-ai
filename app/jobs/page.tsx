"use client";
import {useState} from "react";
const types=["research","content","voice","visuals","render","thumbnail","seo","publish"];
export default function Jobs(){const [jobs,setJobs]=useState<any[]>([]);async function add(type:string){const r=await fetch("/api/jobs",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type})});const j=await r.json();setJobs(x=>[j,...x])}return <main className="container"><nav className="nav"><div className="logo">NOVYN</div><span className="muted">Job Center</span></nav><section style={{padding:"45px 0"}}><span className="badge">AUTOMATION QUEUE</span><h1 style={{fontSize:42}}>Production jobs</h1><p className="muted">Every generation stage is represented as a trackable job with retry limits.</p><div className="grid">{types.map(t=><article className="card" key={t}><h3>{t}</h3><button className="cta" style={{border:0,cursor:"pointer"}} onClick={()=>add(t)}>Queue job</button></article>)}</div><div style={{marginTop:28}}>{jobs.map(j=><article className="card" key={j.id} style={{marginTop:12}}><b>{j.type}</b><span className="muted"> — {j.status} — {j.id}</span></article>)}</div></section></main>}