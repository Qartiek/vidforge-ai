"use client";
import {useState} from "react";
const stages=["Voice-over","Visuals","Editing","Thumbnail","SEO","Export"];
export default function Production(){const [company,setCompany]=useState("");const [result,setResult]=useState<any>(null);async function run(){const r=await fetch("/api/production",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({company})});setResult(await r.json())}return <main className="container"><nav className="nav"><div className="logo">VidForge AI</div><span className="muted">Production Studio</span></nav><section style={{padding:"45px 0"}}><span className="badge">VIDEO PRODUCTION</span><h1 style={{fontSize:42}}>Build the finished video.</h1><p className="muted">Plan voice, visuals, editing, thumbnails, SEO and export from one production workspace.</p><div className="card" style={{maxWidth:760,marginTop:28}}><input value={company} onChange={e=>setCompany(e.target.value)} placeholder="Company / project name" style={{width:"100%",padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><button className="cta" style={{border:0,marginTop:16,cursor:"pointer"}} onClick={run} disabled={!company.trim()}>Create Production Plan</button></div>{result?.plan&&<div className="grid">{stages.map((s,i)=><article className="card" key={s}><small className="muted">STAGE {i+1}</small><h3>{s}</h3><p className="muted">Ready for provider integration.</p></article>)}</div>}</section></main>}