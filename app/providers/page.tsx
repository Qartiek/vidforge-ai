"use client";
import {useEffect,useState} from "react";
export default function Providers(){const [data,setData]=useState<any>();useEffect(()=>{fetch("/api/providers").then(r=>r.json()).then(setData)},[]);return <main className="container"><nav className="nav"><div className="logo">VidForge AI</div><span className="muted">AI Providers</span></nav><section style={{padding:"45px 0"}}><span className="badge">PROVIDER CONTROL</span><h1 style={{fontSize:42}}>AI provider layer</h1><p className="muted">Keys remain server-side. Provider availability can be checked without exposing secrets.</p><div className="grid">{["openai","anthropic","google"].map(p=><article className="card" key={p}><h3>{p}</h3><p className="muted">{data?.providers?.[p]?.configured?"Configured":"Not configured"}</p></article>)}</div></section></main>