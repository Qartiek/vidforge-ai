"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const router=useRouter();const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password}),cache:"no-store"});const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.error||"Login failed");return;}router.replace("/dashboard");router.refresh();}catch{setError("Unable to reach the login service. Please try again.");}finally{setBusy(false);}}
return <main className="container"><section className="hero"><span className="badge">VIDFORGE AI</span><h1>Welcome back.</h1><p>Sign in to access your content automation workspace.</p><form className="card" style={{maxWidth:460,margin:"30px auto",textAlign:"left"}} onSubmit={submit}><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email" style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><label style={{display:"block",marginTop:16}}>Password</label><input required minLength={12} type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/>{error&&<p role="alert" style={{color:"#f87171"}}>{error}</p>}<button className="cta" disabled={busy} style={{border:0,marginTop:16,cursor:"pointer"}}>{busy?"Signing in…":"Sign in"}</button><p className="muted" style={{marginTop:18}}>New here? <a href="/signup" style={{color:"#a99cff",fontWeight:700}}>Create an account</a></p></form></section></main>}