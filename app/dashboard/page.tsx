"use client";
import {useState} from "react";
const steps=["Research","Hook","Script","Voice","Visuals","Edit","Thumbnail","SEO","Publish"];
export default function Dashboard(){const [company,setCompany]=useState("");const [started,setStarted]=useState(false);return <main className="container"><nav className="nav"><div className="logo">VidForge AI</div><span className="muted">Creator Dashboard</span></nav><section style={{padding:"45px 0"}}><span className="badge">WORKSPACE</span><h1 style={{fontSize:42}}>Create your next video</h1><p className="muted">Give VidForge a topic or company. The automation pipeline will handle the production workflow.</p><div className="card" style={{maxWidth:760,marginTop:28}}><label>Topic / Company</label><input value={company} onChange={e=>setCompany(e.target.value)} placeholder="e.g. NVIDIA — How does it make money?" style={{width:"100%",marginTop:10,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><button onClick={()=>setStarted(true)} className="cta" style={{border:0,marginTop:16,cursor:"pointer"}}>Start AI Workflow</button></div>{started&&<section style={{marginTop:32}}><h2>Production Pipeline</h2><div className="grid">{steps.map((s,i)=><article className="card" key={s}><small className="muted">STEP {i+1}</small><h3>{s}</h3><p className="muted">{i===0?"Research and source verification":"Ready for automation"}</p></article>)}</div></section>}</section></main>}