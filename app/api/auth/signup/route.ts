import {NextResponse} from "next/server";import {registerUser,createSession} from "../../../../lib/auth";export async function POST(request:Request){const body=await request.json().catch(()=>null);try{if(typeof body?.email!=="string"||typeof body?.password!=="string")throw new Error();const user=await registerUser(body.email,body.password,typeof body.name==="string"?body.name:undefined);await createSession(user);return NextResponse.json({ok:true,user},{status:201});}catch{return NextResponse.json({error:"Unable to create account"},{status:400});}}