"use client";
import {useState} from "react";
export default function Research(){const [company,setCompany]=useState("");const [angle,setAngle]=useState("");const [result,setResult]=useState<any>(null);async function run(){const r=await fetch("/api/research",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({company,angle})});setResult(await r.json())}return <main className="container"><nav className="nav"><div className="logo">VidForge AI</div><span className="muted">Research Engine</span></nav><section style={{padding:"45px 0"}}><span className="badge">SOURCE-AWARE RESEARCH</span><h1 style={{fontSize:42}}>Research before you write.</h1><p className="muted">Generate a structured research plan designed around primary sources, cross-checking and claim verification.</p><div className="card" style={{maxWidth:760,marginTop:28}}><input value={company} onChange={e=>setCompany(e.target.value)} placeholder="Company name" style={{width:"100%",padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><input value={angle} onChange={e=>setAngle(e.target.value)} placeholder="Analysis angle" style={{width:"100%",marginTop:12,padding:15,borderRadius:10,border:"1px solid #334155",background:"#080d18",color:"#fff"}}/><button className="cta" style={{border:0,marginTop:16,cursor:"pointer"}} onClick={run} disabled={!company.trim()}>Build Research Plan</button></div>{result?.plan&&<div className="card" style={{maxWidth:760,marginTop:24}}><h3>Research queries</h3>{result.plan.queries.map((q:string)=><p className="muted" key={q}>• {q}</p>)}<h3>Evidence rules</h3>{result.plan.rules.map((x:string)=><p className="muted" key={x}>• {x}</p>)}</div>}</section></main>}