export type RenderManifest={version:1;format:"mp4";width:1920;height:1080;fps:30;audio:{source:"tts";codec:"aac"};scenes:{index:number;start:number;end:number;assetUrl:string}[]};
export function buildRenderManifest(duration:number,assets:{sceneIndex:number|null;url:string}[]):RenderManifest{const scenes=assets.filter(a=>a.sceneIndex!==null).sort((a,b)=>(a.sceneIndex??0)-(b.sceneIndex??0)).map((a,i)=>({index:a.sceneIndex??i,start:0,end:0,assetUrl:a.url}));const total=Math.max(1,duration);const count=Math.max(1,scenes.length);return {version:1,format:"mp4",width:1920,height:1080,fps:30,audio:{source:"tts",codec:"aac"},scenes:scenes.map((s,i)=>({...s,start:Math.round(i*total/count),end:Math.round((i+1)*total/count)}))};}
export function ffmpegCommand(manifestPath:string,outputPath:string){return ["ffmpeg","-hide_banner","-y","-i",manifestPath,"-c:v","libx264","-preset","medium","-crf","20","-c:a","aac","-movflags","+faststart",outputPath];}
