import {createHash} from "crypto";
export type SourceInput={url:string;title?:string;publisher?:string};
export function normalizeUrl(value:string){const u=new URL(value);if(!["http:","https:"].includes(u.protocol))throw new Error("Only HTTP(S) sources are allowed");u.hash="";return u.toString();}
export async function fetchSource(input:SourceInput){const url=normalizeUrl(input.url);const response=await fetch(url,{redirect:"follow",headers:{"user-agent":"VidForgeAI-Research/1.0"},signal:AbortSignal.timeout(12_000)});if(!response.ok)throw new Error("Source fetch failed");const type=response.headers.get("content-type")||"";if(!type.includes("text/html")&&!type.includes("text/plain")&&!type.includes("application/json"))throw new Error("Unsupported source type");const raw=(await response.text()).slice(0,200_000);const content=raw.replace(/<script[\s\S]*?<\/script>/gi," ").replace(/<style[\s\S]*?<\/style>/gi," ").replace(/<[^>]+>/g," ").replace(/\s+/g," ").trim();return {url,title:input.title||null,publisher:input.publisher||new URL(url).hostname,content,contentHash:createHash("sha256").update(content).digest("hex")};}
