export type ContentBrief={topic:string;audience:string;durationMinutes:number;tone:string;language:string;platform:string;format:string};
export type GeneratedContent={hook:string;title:string;script:string;seoTitle:string;description:string;tags:string[];thumbnailConcepts:string[];shortFormHooks:string[];cta:string};
export function buildContentPrompt(brief:ContentBrief){return `You are VidForge AI's senior content strategist, researcher and retention editor. Create a production-ready ${brief.format} for ${brief.platform}. Topic: ${brief.topic}. Audience: ${brief.audience}. Duration: ${brief.durationMinutes} minutes. Tone: ${brief.tone}. Language: ${brief.language}. First design the audience angle and value proposition internally. Then write a compelling opening hook as the first lines of the final script. The hook is PART OF THE SCRIPT, never a separate production stage. Use clear spoken language, pattern interrupts, curiosity gaps only when honestly resolved, retention beats, examples, transitions, a useful conclusion and natural CTA. Create strong title options, SEO metadata, thumbnail concepts and short-form repurposing hooks. Avoid clickbait that misrepresents the content. Never invent facts, statistics, quotes, sources, or current events. If factual verification is required, mark the claim [VERIFY]. Return valid JSON only with keys: hook,title,script,seoTitle,description,tags,thumbnailConcepts,shortFormHooks,cta.`}
export function buildWorkflowStages(){return ["idea","research","content_angle","script_with_hook","title_thumbnail","voiceover","visuals","editing","captions","seo","shorts_reels","quality_check","schedule_publish","analytics_optimize"]}
