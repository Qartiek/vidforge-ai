export type ContentBrief={topic:string;audience:string;durationMinutes:number;tone:string;language:string;platform:string;format:string};
export type GeneratedContent={hook:string;title:string;script:string;seoTitle:string;description:string;tags:string[];thumbnailConcepts:string[];shortFormHooks:string[];cta:string};
export function buildContentPrompt(brief:ContentBrief){return `Create a production-ready ${brief.format} for ${brief.platform}. Topic: ${brief.topic}. Audience: ${brief.audience}. Duration: ${brief.durationMinutes} minutes. Tone: ${brief.tone}. Language: ${brief.language}. The hook MUST be part of the beginning of the final script, not a separate production stage. Create a strong opening, natural spoken-language script, retention beats, useful value, CTA, SEO metadata, thumbnail concepts, and short-form repurposing hooks. Never invent facts, statistics, quotes, or current events. If factual verification is required, mark the claim [VERIFY]. Return valid JSON only with keys: hook,title,script,seoTitle,description,tags,thumbnailConcepts,shortFormHooks,cta.`}
export function buildWorkflowStages(){return ["idea","research","script_with_hook","title_thumbnail","voiceover","visuals","editing","captions","seo","shorts_reels","quality_check","schedule_publish","analytics_optimize"]}
