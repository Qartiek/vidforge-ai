export type MediaAsset={type:"audio"|"image"|"video";url:string;duration?:number;provider:string;metadata?:Record<string,string>};
export function buildProductionPlan(script:string){const words=script.trim().split(/\s+/).length;const duration=Math.max(30,Math.round(words/2.4));const scenes=Math.max(6,Math.ceil(duration/8));return {durationSeconds:duration,scenes:Array.from({length:scenes},(_,i)=>({index:i,kind:"visual",start:Math.round(i*duration/scenes),end:Math.round((i+1)*duration/scenes),prompt:"Finance documentary visual matching the verified script; no invented numbers or logos."}))};}
